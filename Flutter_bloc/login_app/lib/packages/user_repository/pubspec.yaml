name: test_extension
description: the Repo that handles users.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1



environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase Authentication
  firebase_auth: ^5.3.1 # You can replace with the latest version

  # Cloud Firestore
  cloud_firestore: ^5.4.4
  test_extension: any   # You can replace with the latest version

  # Flutter Bloc
  flutter_bloc: ^8.1.3

  equatable: ^2.0.5
  cupertino_icons: ^1.0.5
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.0.0
  bloc: ^8.1.4
  equatable: ^2.0.5 # You can replace with the latest version


dev_dependencies:
  flutter_test:
    sdk: flutter
  mason_cli: 0.1.0-dev.57

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true